# Secrets — never commit to a public repo
.env
.env.*
!.env.example
.password-overrides.json
!.password-overrides.example.json
*.pem
*.key
*-credentials.json
*-service-account.json
secrets/
private/

# StatiCrypt salt cache — derived from master secret, don't publish
.staticrypt.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Logs / temp
*.log
.cache/
node_modules/

# Plaintext deliverable sources - NEVER commit to this repo.
# It is public and Pages serves it from root, so anything here is readable at
# /public-encrypted/<path> regardless of the StatiCrypt gate on the public/ copy.
# Sources live in the private Team repo at ../web-shared-sources/.
public-encrypted/
